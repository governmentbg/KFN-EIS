//KEEP IT FOR FUTURE TESTS


// import { store } from 'store';
// import { ModalNames } from 'store/slices/modals';
// import checkIsModalOpen from './checkIsModalOpen';

// // ---------- functions ----------
// describe('functions', () => {
//   // checkIsModalOpen
//   it('checkIsModalOpen should check if modal is not open correctly', () => {
//     const state = store.getState();
//     const modals = state?.modals;
//     const result = checkIsModalOpen(modals, ModalNames.CONFIRM_MODAL);

//     expect(result).toEqual(false);
//   });

//   it('checkIsModalOpen should check if modal is open correctly', () => {
//     const modals = [{ name: ModalNames.CONFIRM_MODAL, isOpen: true }];
//     const result = checkIsModalOpen(modals, ModalNames.CONFIRM_MODAL);

//     expect(result).toEqual(true);
//   });
// });
// // ---------- end functions ----------

describe('fake', () => {
  it('fake', () => {
    expect(true);
  });
});
