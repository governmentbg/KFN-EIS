import Typography from "@mui/material/Typography"
import DefaultLayout from "../../../../layouts/DefaultLayout"

const KEPAuthPage = () => {
    return <DefaultLayout>
        <Typography component='h1' variant="h2">Автентикация с КЕП</Typography>
        В момента услугата не е достъпна поради липса на имплементация.
    </DefaultLayout>
}

export default KEPAuthPage